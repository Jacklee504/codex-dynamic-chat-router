---
description: Abort a routing run and start fresh subagents for a corrected objective. Use when the user explicitly asks to restart routed work.
disable-model-invocation: true
---

# Restart

Run the abort procedure first. Confirm active subagents stopped, then route
fresh subagents with the corrected objective. Never repurpose a stopped or
completed subagent for a changed task.
