---
description: Abort a routing run and start fresh workers for a corrected objective. Use when the user explicitly asks to restart routed work.
disable-model-invocation: true
---

# Restart

Run the abort procedure first. Confirm active workers stopped, then route fresh
workers with the corrected objective. Never repurpose a stopped or completed
worker for a changed task.
