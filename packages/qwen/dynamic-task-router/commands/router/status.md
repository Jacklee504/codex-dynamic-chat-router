---
description: Report routed Qwen Code subagents, blockers, and next owner without changing work.
---

Read the current routed subagents only. Return one line per subagent:

```text
<subagent> — <state> — <blocker or next action>
```

Do not dispatch, redirect, resume, or stop a subagent.
