---
description: Report routed Qwen Code workers, blockers, and next owner without changing work.
---

Read the current routed workers only. Return one line per worker:

```text
<worker> — <state> — <blocker or next action>
```

Do not dispatch, redirect, resume, or stop a worker.
