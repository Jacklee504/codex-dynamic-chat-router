interface:
  display_name: "App Subchat Router"
  short_description: "Route bounded work through Codex subchats."
  default_prompt: "Use $app-subchat-router to delegate a bounded implementation safely."
policy:
  allow_implicit_invocation: false
